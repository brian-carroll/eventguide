{
  "name": "eventguide",
  "version": "1.0.0",
  "description": "Event guide website",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "dependencies": {
  },
  "devDependencies": {
    "elm-test": "^0.17.3"
  },
  "scripts": {
    "clean": "rm -rf tests/elm-stuff/build-artifacts/*/user ; rm -rf elm-stuff/build-artifacts/*/user",
    "test": "npm run clean ; elm test",
    "build": "rm -rf assets/js/elm.* && elm make src/Main.elm --output assets/js/elm.js",
    "compress": "closure-compiler --compilation_level ADVANCED_OPTIMIZATIONS --language_in ECMASCRIPT5 --js assets/js/elm.js > assets/js/elm.min.js && gzip -fk assets/js/elm.min.js && ls -lh assets/js"
  },
  "author": "",
  "license": "ISC"
}
